/* eslint-disable react-refresh/only-export-components */
import transition from "../../animations/transition"

const NotFoundPage = () => {
  return <div>Ошибка 404: Извините, данной страницы не существует</div>
}

export default transition(NotFoundPage)
